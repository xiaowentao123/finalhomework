import React, { forwardRef } from 'react';
export function Spinner(StyledSpinner) {
    return forwardRef(({ ...props }, ref) => {
        return (<StyledSpinner {...props} ref={ref} tabIndex={-1} aria-label="loading"/>);
    });
}
