import React, { memo } from 'react';
import { forwardRef } from 'react';
export const TabIcon = (StyledTabIcon) => memo(forwardRef(({ ...props }, ref) => {
    return <StyledTabIcon {...props} ref={ref}/>;
}));
