import React, { memo } from 'react';
import { forwardRef } from 'react';
export const TabTitle = (StyledTabTitle) => memo(forwardRef(({ ...props }, ref) => {
    return <StyledTabTitle {...props} ref={ref}/>;
}));
