import React, { forwardRef } from 'react';
export function TooltipArrow(StyledTooltipArrow) {
    return forwardRef(({ children, ...props }, ref) => {
        return (<StyledTooltipArrow ref={ref} {...props}>
        {children}
      </StyledTooltipArrow>);
    });
}
