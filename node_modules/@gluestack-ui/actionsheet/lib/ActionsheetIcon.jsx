import React, { forwardRef } from 'react';
export const ActionsheetIcon = (StyledActionsheetIcon) => forwardRef(({ children, ...props }, ref) => {
    return (<StyledActionsheetIcon {...props} ref={ref}>
        {children}
      </StyledActionsheetIcon>);
});
