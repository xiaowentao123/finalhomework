import React, { forwardRef } from 'react';
export function ActionsheetScrollView(StyledActionsheetScrollView) {
    return forwardRef(({ children, ...props }, ref) => {
        return (<StyledActionsheetScrollView ref={ref} {...props}>
          {children}
        </StyledActionsheetScrollView>);
    });
}
