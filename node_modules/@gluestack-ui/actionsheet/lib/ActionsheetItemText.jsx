import React, { forwardRef } from 'react';
export function ActionsheetItemText(StyledActionsheetText) {
    return forwardRef(({ children, ...props }, ref) => {
        return (<StyledActionsheetText ref={ref} {...props}>
          {children}
        </StyledActionsheetText>);
    });
}
