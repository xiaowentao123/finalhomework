import React, { forwardRef } from 'react';
export const SelectIcon = (StyledSelectIcon) => forwardRef(({ children, ...props }, ref) => {
    return (<StyledSelectIcon tabIndex={-1} {...props} ref={ref}>
        {children}
      </StyledSelectIcon>);
});
